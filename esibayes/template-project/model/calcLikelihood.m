function objScore = <calcLikelihood>(conf,constants,modelOutput,parVec)
mmsodaUnpack()
% don't fiddle with the first two lines, these are always the same
% calculate the log likelihood of your model's prediction

dens = something;

objScore = log(dens);
